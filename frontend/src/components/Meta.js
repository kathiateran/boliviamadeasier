import React from 'react'
import { Helmet } from 'react-helmet'

const Meta = ({ title, description, keywords }) => {
  return (
    <Helmet>
      <title>{title}</title>
      <meta name='description' content={description} />
      <meta name='keyword' content={keywords} />
    </Helmet>
  )
}

Meta.defaultProps = {
  title: 'Welcome To Bolivia Made Easier',
  description: 'We offer the best tours in Bolivia!',
  keywords: 'south america, south america tourism, bolivia tourism',
}

export default Meta
